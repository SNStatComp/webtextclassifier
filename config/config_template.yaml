filesystem:
  endpoint:
  bucket:
aws:
  access_key:
  secret_access_key:
  session_token:
  default_region:
llm:
  method:
  prompt:
    model:
    api_key:
    api_url:
  embedding:
    model: 
    model_weights: 
    max_nchar: 
    dim: 
    classifier: 
    classifier_path: 
requests:
  timeout: 60
  default_language: English
input:
  input_dir: input
  input_file: 
output:
  output_dir: output
seed:
batchsize: